# Times Agent Policy Site

GitHub Pages source for the Teams app public URLs.

Published from the repository root on the `main` branch.

Pages:

- `/` — overview/support
- `/privacy/` — privacy
- `/terms/` — terms

The old `/tab/` Teams guide was removed when the Teams app moved to the v0.2.0 bot-only, team-scope manifest.

Main application repository: `TimesAgent`.
